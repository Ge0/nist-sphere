CMAKE_MINIMUM_REQUIRED(VERSION 3.10.0)
PROJECT(sphere VERSION 2.6.1 LANGUAGES C)


INCLUDE_DIRECTORIES(include)
ADD_SUBDIRECTORY(src)

INSTALL(
	DIRECTORY ${CMAKE_SOURCE_DIR}/include/
	DESTINATION include
	FILES_MATCHING PATTERN "*.h"
)
